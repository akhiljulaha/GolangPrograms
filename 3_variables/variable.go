package main

import "fmt"

func main() {
	// var name string = "golang" 
	var name = "golang" // short way to right(golang gets to know the data type automatically)
	// var isAdult bool = true
	// var age int = 33

	// shorthand syntax (shortcut for the above lines)
	// name := "golang"    // this we can use only inside the main function
	
	// var price = 50.44

	fmt.Println(name)




}
//in the golang if creating a variable so that must be used, if it unused so have to delete
